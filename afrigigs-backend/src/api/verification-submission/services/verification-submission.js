'use strict';

/**
 * verification-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::verification-submission.verification-submission');
