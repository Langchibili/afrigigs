'use strict';

/**
 * verification-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::verification-submission.verification-submission');
