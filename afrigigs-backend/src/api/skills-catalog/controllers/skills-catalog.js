'use strict';

/**
 * skills-catalog controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::skills-catalog.skills-catalog');
